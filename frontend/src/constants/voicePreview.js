/**
 * Default text used for voice preview audio (e.g. when no sampleText is provided).
 * Single source of truth for frontend voice preview fallbacks.
 * Kept short to minimize payload size when using inline base64 (Option A).
 */
export const DEFAULT_VOICE_PREVIEW_TEXT =
  "Hello, this is Robert from Universal Motorcycle Training. How can I help you today?";
